name: 🚀 Crear Backlog y Sprints - BrainBlitz Vision Computacional

on:
  workflow_dispatch: # Permite ejecutar este workflow manualmente desde la pestaña Actions

# Define el token con permisos de escritura
permissions:
  contents: write # ⚠️ Se cambió a 'write' para poder crear ramas
  issues: write
  # Es necesario añadir permisos para 'pull-requests: write' para que pueda crear etiquetas
  pull-requests: write

jobs:
  setup-project-backlog:
    runs-on: ubuntu-latest
    steps:
      # PASO 1: Descarga el código del repositorio
      - name: Checkout repository
        uses: actions/checkout@v4

      # PASO 2: Crea el proyecto si no existe y obtiene su número automáticamente
      - name: Crear Proyecto de GitHub si no existe
        id: project_setup # Le damos un ID a este paso para usar sus resultados
        env:
          # ⚠️ MODIFICACIÓN AÑADIDA: Usa el Token de Acceso Personal para tener permisos
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          PROJECT_NAME="BrainBlitz - Computer Vision Backlog"
          # ⚠️ MODIFICACIÓN AÑADIDA: Comando corregido para buscar el proyecto
          PROJECT_NUMBER=$(gh project list --owner "@me" --format json | jq -r ".projects[] | select(.title == \"$PROJECT_NAME\") | .number")
          
          if [ -z "$PROJECT_NUMBER" ]; then
            echo "Proyecto '$PROJECT_NAME' no encontrado. Creándolo ahora..."
            # Crea el proyecto y extrae su número de la URL resultante
            PROJECT_URL=$(gh project create --owner "@me" --title "$PROJECT_NAME")
            PROJECT_NUMBER=$(echo "$PROJECT_URL" | rev | cut -d'/' -f1 | rev)
            echo "Proyecto creado con el número: $PROJECT_NUMBER"
          else
            echo "Proyecto '$PROJECT_NAME' ya existe con el número: $PROJECT_NUMBER"
          fi
          # Guarda el número del proyecto para que otros pasos puedan usarlo
          echo "PROJECT_NUMBER=$PROJECT_NUMBER" >> $GITHUB_OUTPUT

      # PASO 3: Crea las Etiquetas de Prioridad y Tecnología si no existen
      - name: Crear Etiquetas si no existen
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }} # Usamos el PAT para consistencia
        run: |
          echo "Creando etiquetas de prioridad..."
          gh label create "Prioridad: Alta" --color "D93F0B" --description "Tarea crítica y bloqueante." || echo "La etiqueta 'Prioridad: Alta' ya existe."
          gh label create "Prioridad: Media" --color "FBCA04" --description "Tarea importante pero no urgente." || echo "La etiqueta 'Prioridad: Media' ya existe."
          gh label create "Prioridad: Baja" --color "0E8A16" --description "Tarea menor o mejora deseable." || echo "La etiqueta 'Prioridad: Baja' ya existe."
          
          echo "Creando etiquetas de tecnología..."
          gh label create "Tecnología: Azure" --color "0078D4" --description "Usa servicios de Azure" || echo "La etiqueta 'Tecnología: Azure' ya existe."
          gh label create "Tecnología: DeepFace" --color "6F42C1" --description "Usa DeepFace para reconocimiento facial" || echo "La etiqueta 'Tecnología: DeepFace' ya existe."
          gh label create "Tecnología: Firebase" --color "FF6D01" --description "Integración con Firebase" || echo "La etiqueta 'Tecnología: Firebase' ya existe."
          gh label create "IA: Visión Computacional" --color "00B294" --description "Implementa visión computacional" || echo "La etiqueta 'IA: Visión Computacional' ya existe."

      # PASO 4: Crea los Milestones (Sprints) solo si no existen
      - name: Crear Milestones si no existen
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }} # Usamos el PAT para consistencia
          GH_REPO: ${{ github.repository }}
        run: |
          # --- Sprint 1: Autenticación y Extracción ---
          MILESTONE_SPRINT1_TITLE="Sprint 1: Autenticación y Extracción (17-19 Noviembre)"
          if gh api /repos/${GH_REPO}/milestones --paginate -q ".[] | select(.title == \"$MILESTONE_SPRINT1_TITLE\")" | grep -qF "$MILESTONE_SPRINT1_TITLE"; then
            echo "Milestone '$MILESTONE_SPRINT1_TITLE' ya existe. Omitiendo."
          else
            echo "Creando milestone '$MILESTONE_SPRINT1_TITLE'..."
            gh api --method POST -H "Accept: application/vnd.github+json" "/repos/${GH_REPO}/milestones" \
              -f title="$MILESTONE_SPRINT1_TITLE" \
              -f description='Implementar OCR y Reconocimiento Facial. Total: 21 Story Points' \
              -f due_on='2025-11-19T23:59:59Z'
          fi

          # --- Sprint 2: Análisis Visual Avanzado ---
          MILESTONE_SPRINT2_TITLE="Sprint 2: Análisis Visual Avanzado (20-24 Noviembre)"
          if gh api /repos/${GH_REPO}/milestones --paginate -q ".[] | select(.title == \"$MILESTONE_SPRINT2_TITLE\")" | grep -qF "$MILESTONE_SPRINT2_TITLE"; then
            echo "Milestone '$MILESTONE_SPRINT2_TITLE' ya existe. Omitiendo."
          else
            echo "Creando milestone '$MILESTONE_SPRINT2_TITLE'..."
            gh api --method POST -H "Accept: application/vnd.github+json" "/repos/${GH_REPO}/milestones" \
              -f title="$MILESTONE_SPRINT2_TITLE" \
              -f description='Implementar análisis de imágenes y detección de objetos. Total: 20 Story Points' \
              -f due_on='2025-11-24T23:59:59Z'
          fi

      # PASO 5: Crea todos los Issues, los añade al proyecto y solo si no existen
      - name: Crear Issues y añadirlas al Proyecto
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }} # Usamos el PAT para consistencia
          PROJECT_NUMBER: ${{ steps.project_setup.outputs.PROJECT_NUMBER }}
        run: |
          # Obtener los IDs del campo Status y opción Todo dinámicamente
          echo "Obteniendo IDs del proyecto..."
          PROJECT_ID=$(gh project view ${{ env.PROJECT_NUMBER }} --owner "@me" --format json | jq -r '.id')
          
          # Obtener el campo Status del proyecto
          STATUS_FIELD_ID=$(gh api graphql -f query='
            query($projectId: ID!) {
              node(id: $projectId) {
                ... on ProjectV2 {
                  fields(first: 20) {
                    nodes {
                      ... on ProjectV2Field {
                        id
                        name
                      }
                      ... on ProjectV2SingleSelectField {
                        id
                        name
                        options {
                          id
                          name
                        }
                      }
                    }
                  }
                }
              }
            }
          ' -f projectId="$PROJECT_ID" | jq -r '.data.node.fields.nodes[] | select(.name == "Status") | .id')
          
          # Obtener el ID de la opción "Todo"
          TODO_COLUMN_ID=$(gh api graphql -f query='
            query($projectId: ID!) {
              node(id: $projectId) {
                ... on ProjectV2 {
                  fields(first: 20) {
                    nodes {
                      ... on ProjectV2SingleSelectField {
                        id
                        name
                        options {
                          id
                          name
                        }
                      }
                    }
                  }
                }
              }
            }
          ' -f projectId="$PROJECT_ID" | jq -r '.data.node.fields.nodes[] | select(.name == "Status") | .options[] | select(.name == "Todo") | .id')
          
          echo "IDs obtenidos: PROJECT=$PROJECT_ID, STATUS_FIELD=$STATUS_FIELD_ID, TODO_OPTION=$TODO_COLUMN_ID"
          
          # Si no se encontró el campo Status o la opción Todo, continuar sin mover las issues
          if [ -z "$STATUS_FIELD_ID" ] || [ -z "$TODO_COLUMN_ID" ]; then
            echo "⚠️ No se pudo encontrar el campo Status o la opción Todo. Las issues se crearán pero no se moverán."
            MOVE_TO_TODO=false
          else
            MOVE_TO_TODO=true
          fi

          # La función ahora también añade la issue al proyecto.
          create_and_add_to_project() {
            local title="$1"
            local body="$2"
            local label="$3"
            local milestone="$4"
            
            ISSUE_URL=$(gh issue list --state all --search "in:title \"$title\"" --json url -q ".[0].url")

            if [ -n "$ISSUE_URL" ]; then
              echo "Issue '$title' ya existe. Omitiendo creación."
            else
              echo "Creando issue '$title'..."
              ISSUE_URL=$(gh issue create --title "$title" --body "$body" --label "$label" --milestone "$milestone")
            fi

            if [ -n "$ISSUE_URL" ]; then
                echo "Añadiendo issue $ISSUE_URL al proyecto ${{ env.PROJECT_NUMBER }}..."
                CONTENT_ID=$(gh issue view $ISSUE_URL --json id | jq -r '.id')

                # Añade la issue al proyecto y obtiene el ID del item
                ITEM_ID=$(gh api graphql -f query='
                  mutation($project:ID!, $content:ID!) {
                    addProjectV2ItemById(input: {projectId: $project, contentId: $content}) {
                      item { id }
                    }
                  }
                ' -f project="$PROJECT_ID" -f content="$CONTENT_ID" | jq -r '.data.addProjectV2ItemById.item.id')

                # Mover a Todo solo si se encontraron los IDs
                if [ "$MOVE_TO_TODO" = true ] && [ -n "$ITEM_ID" ]; then
                  echo "Moviendo issue a Todo..."
                  
                  gh api graphql -f query="
                    mutation {
                      updateProjectV2ItemFieldValue(input: {
                        projectId: \"$PROJECT_ID\",
                        itemId: \"$ITEM_ID\",
                        fieldId: \"$STATUS_FIELD_ID\",
                        value: {singleSelectOptionId: \"$TODO_COLUMN_ID\"}
                      }) {
                        projectV2Item { id }
                      }
                    }
                  " && echo "✅ Issue movida a Todo" || echo "⚠️ No se pudo mover la issue a Todo"
                else
                  echo "⚠️ No se pudo mover la issue a Todo (IDs no encontrados)"
                fi
            fi
          }

          # ===============================================
          # ==         ISSUES SPRINT 1                  ==
          # ===============================================

          create_and_add_to_project \
            "HU-VC2: [BE] OCR - Extracción de Texto de Imágenes" \
            "**Como** usuario o administrador de BrainBlitz, **Quiero** poder extraer texto automáticamente de imágenes, **Para que** pueda generar preguntas de forma más eficiente.

            ---
            ### **Puntos de Estimación:** 8
            ---
            ### **Criterios de Aceptación:**
            - ✅ Configurar Azure Computer Vision con API Key y Endpoint
            - ✅ Crear endpoint POST /api/vision/extract-text
            - ✅ Integrar con Azure OCR v3.2 para procesamiento asíncrono
            - ✅ Desarrollar componente OCRQuestionCreator.jsx en frontend
            - ✅ Permitir subida de imágenes y preview
            - ✅ Mostrar texto extraído en textarea editable
            - ✅ Integrar con AIQuestionGenerator.jsx
            - ✅ Realizar pruebas con JPG, PNG, PDF

            ### **Tecnologías:**
            - Azure Computer Vision OCR
            - Node.js, Express, React
            - Base64 encoding

            ### **Dependencias:**
            - Configuración de Azure Computer Vision
            - Variables: AZURE_COMPUTER_VISION_KEY, AZURE_COMPUTER_VISION_ENDPOINT" \
            "Prioridad: Media,IA: Visión Computacional,Tecnología: Azure" "Sprint 1: Autenticación y Extracción (17-19 Noviembre)"

          create_and_add_to_project \
            "HU-VC1: [BE] Reconocimiento Facial para Login y Registro" \
            "**Como** usuario de BrainBlitz, **Quiero** poder autenticarme usando reconocimiento facial, **Para que** tenga una experiencia más segura y moderna.

            ---
            ### **Puntos de Estimación:** 13
            ---
            ### **Criterios de Aceptación:**
            - ✅ Crear endpoints POST /api/face/register y POST /api/face/login
            - ✅ Implementar validación de rostros con DeepFace
            - ✅ Generar embeddings faciales con modelo VGG-Face
            - ✅ Almacenar embeddings en Firestore asociados a userId
            - ✅ Desarrollar componentes FaceRegister.jsx y FaceLogin.jsx
            - ✅ Integrar vista previa de cámara en tiempo real
            - ✅ Configurar microservicio facial en Azure Container Instances
            - ✅ Implementar rate limiting y seguridad
            - ✅ Realizar pruebas de autenticación biométrica

            ### **Tecnologías:**
            - DeepFace, VGG-Face
            - Azure Container Instances
            - Firebase Auth
            - React, getUserMedia API

            ### **Dependencias:**
            - Microservicio facial desplegado
            - Firebase Auth configurado
            - Azure Container Instances" \
            "Prioridad: Alta,IA: Visión Computacional,Tecnología: DeepFace,Tecnología: Firebase" "Sprint 1: Autenticación y Extracción (17-19 Noviembre)"

          # ===============================================
          # ==         ISSUES SPRINT 2                  ==
          # ===============================================

          create_and_add_to_project \
            "HU-VC3: [BE] Análisis Inteligente de Imágenes" \
            "**Como** usuario o administrador de BrainBlitz, **Quiero** analizar imágenes automáticamente, **Para que** pueda generar preguntas basadas en contenido visual.

            ---
            ### **Puntos de Estimación:** 10
            ---
            ### **Criterios de Aceptación:**
            - ✅ Crear endpoint POST /api/vision/analyze-image
            - ✅ Integrar con Azure Analyze Image API
            - ✅ Extraer descripción, tags, categorías, objetos y colores
            - ✅ Ordenar resultados por confianza descendente
            - ✅ Desarrollar componente ImageAnalysisQuestionCreator.jsx
            - ✅ Mostrar resultados en secciones organizadas
            - ✅ Implementar generación automática de preguntas
            - ✅ Pre-llenar formularios con sugerencias
            - ✅ Realizar pruebas con diferentes tipos de imágenes

            ### **Tecnologías:**
            - Azure Computer Vision Analyze Image
            - Node.js, Express, React

            ### **Dependencias:**
            - Azure Computer Vision configurado
            - Variables: AZURE_COMPUTER_VISION_KEY, AZURE_COMPUTER_VISION_ENDPOINT" \
            "Prioridad: Media,IA: Visión Computacional,Tecnología: Azure" "Sprint 2: Análisis Visual Avanzado (20-24 Noviembre)"

          create_and_add_to_project \
            "HU-VC4: [BE] Detección de Objetos en Imágenes" \
            "**Como** usuario de BrainBlitz, **Quiero** detectar objetos en imágenes, **Para que** pueda crear preguntas visuales interactivas.

            ---
            ### **Puntos de Estimación:** 10
            ---
            ### **Criterios de Aceptación:**
            - ✅ Crear endpoint POST /api/vision/detect-objects
            - ✅ Integrar con Azure Object Detection API
            - ✅ Procesar bounding boxes y niveles de confianza
            - ✅ Desarrollar componente ObjectDetectionQuestionCreator.jsx
            - ✅ Mostrar bounding boxes sobre imágenes
            - ✅ Implementar funcionalidades interactivas (hover, filtros)
            - ✅ Generar preguntas de identificación y conteo automáticamente
            - ✅ Usar Canvas API para visualización
            - ✅ Realizar pruebas de precisión y múltiples objetos

            ### **Tecnologías:**
            - Azure Computer Vision Object Detection
            - Node.js, Express, React
            - Canvas API

            ### **Dependencias:**
            - Azure Computer Vision configurado
            - Variables: AZURE_COMPUTER_VISION_KEY, AZURE_COMPUTER_VISION_ENDPOINT" \
            "Prioridad: Media,IA: Visión Computacional,Tecnología: Azure" "Sprint 2: Análisis Visual Avanzado (20-24 Noviembre)"

      # PASO 6: Crea y Vincula las Ramas para cada Issue
      - name: Crear Ramas y Vincularlas a las Issues
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }} # Usamos el PAT para consistencia
        run: |
          MAIN_BRANCH_SHA=$(git rev-parse main)
          gh issue list --state open --json number,title | jq -c '.[]' | while read issue; do
            ISSUE_NUMBER=$(echo $issue | jq -r '.number')
            ISSUE_TITLE=$(echo $issue | jq -r '.title')
            BRANCH_TITLE=$(echo "$ISSUE_TITLE" | iconv -t ascii//TRANSLIT | sed -E 's/\[(BE|FE)\] //g' | sed -E 's/HU-VC[0-9]+: //g' | sed -E 's/[^a-zA-Z0-9]+/-/g' | sed -E 's/^-+\|-+$//g' | tr '[:upper:]' '[:lower:]')
            BRANCH_NAME="hu-vc${ISSUE_NUMBER}-${BRANCH_TITLE}"

            if git ls-remote --heads origin "$BRANCH_NAME" | grep -q "$BRANCH_NAME"; then
              echo "La rama '$BRANCH_NAME' ya existe."
            else
              echo "Creando rama '$BRANCH_NAME'..."
              gh api repos/${{ github.repository }}/git/refs --method POST -f ref="refs/heads/${BRANCH_NAME}" -f sha="$MAIN_BRANCH_SHA"
              # Comenta en la issue para vincular la rama
              gh issue comment $ISSUE_NUMBER --body "Se ha creado la rama de trabajo para esta HU de Visión Computacional: \`$BRANCH_NAME\`"
            fi
          done